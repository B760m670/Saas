/* Мостик к заголовку ядра.
 *
 * Отдельный файл, а не прямое включение: путь к заголовку задаётся
 * флагом сборки, и держать его в одном месте проще, чем в каждом
 * файле на Swift.
 */
#ifndef CATLAS_SHIM_H
#define CATLAS_SHIM_H

#include "atlas.h"

#endif
